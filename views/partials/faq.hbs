<section class="py-20 bg-white dark:bg-gray-900" x-data="{ openIndex: null }">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    
    <!-- Header -->
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-white mb-4">
        Frequently Asked Questions
      </h2>
      <p class="text-lg text-gray-600 dark:text-gray-400 max-w-2xl mx-auto">
        Everything you need to know about hiring and working with virtual assistants
      </p>
    </div>

    <!-- FAQ Items -->
    <div class="max-w-3xl mx-auto space-y-4">

      <!-- Q1 -->
      <div class="bg-gray-50 dark:bg-gray-800 rounded-xl overflow-hidden border border-gray-200 dark:border-gray-700">
        <button
          @click="openIndex = openIndex === 0 ? null : 0"
          class="w-full px-6 py-5 text-left flex items-center justify-between hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors"
          :aria-expanded="openIndex === 0"
        >
          <span class="font-semibold text-gray-900 dark:text-white">
            How do I hire a virtual assistant?
          </span>
          <svg class="w-5 h-5 text-gray-500 dark:text-gray-400 transform transition-transform"
               :class="openIndex === 0 ? 'rotate-180' : ''"
               fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M19 9l-7 7-7-7"></path>
          </svg>
        </button>

        <div x-show="openIndex === 0" x-collapse>
          <div class="px-6 pb-5 text-gray-600 dark:text-gray-400">
            Simply click on “Hire a VA”, choose the services you need, select a plan, and submit your requirements.
            Our team will match you with a skilled virtual assistant within 24–48 hours.
          </div>
        </div>
      </div>

      <!-- Q2 -->
      <div class="bg-gray-50 dark:bg-gray-800 rounded-xl overflow-hidden border border-gray-200 dark:border-gray-700">
        <button
          @click="openIndex = openIndex === 1 ? null : 1"
          class="w-full px-6 py-5 text-left flex items-center justify-between hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors"
          :aria-expanded="openIndex === 1"
        >
          <span class="font-semibold text-gray-900 dark:text-white">
            What tasks can a virtual assistant handle?
          </span>
          <svg class="w-5 h-5 text-gray-500 dark:text-gray-400 transform transition-transform"
               :class="openIndex === 1 ? 'rotate-180' : ''"
               fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M19 9l-7 7-7-7"></path>
          </svg>
        </button>

        <div x-show="openIndex === 1" x-collapse>
          <div class="px-6 pb-5 text-gray-600 dark:text-gray-400">
            Our virtual assistants can help with administrative support, email management, customer support,
            sales assistance, digital marketing, research, data entry, and e-commerce operations.
          </div>
        </div>
      </div>

      <!-- Q3 -->
      <div class="bg-gray-50 dark:bg-gray-800 rounded-xl overflow-hidden border border-gray-200 dark:border-gray-700">
        <button
          @click="openIndex = openIndex === 2 ? null : 2"
          class="w-full px-6 py-5 text-left flex items-center justify-between hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors"
          :aria-expanded="openIndex === 2"
        >
          <span class="font-semibold text-gray-900 dark:text-white">
            Are your virtual assistants experienced and verified?
          </span>
          <svg class="w-5 h-5 text-gray-500 dark:text-gray-400 transform transition-transform"
               :class="openIndex === 2 ? 'rotate-180' : ''"
               fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M19 9l-7 7-7-7"></path>
          </svg>
        </button>

        <div x-show="openIndex === 2" x-collapse>
          <div class="px-6 pb-5 text-gray-600 dark:text-gray-400">
            Yes. All our virtual assistants go through a strict screening process that includes skill assessments,
            background checks, and interviews to ensure high-quality service.
          </div>
        </div>
      </div>

      <!-- Q4 -->
      <div class="bg-gray-50 dark:bg-gray-800 rounded-xl overflow-hidden border border-gray-200 dark:border-gray-700">
        <button
          @click="openIndex = openIndex === 3 ? null : 3"
          class="w-full px-6 py-5 text-left flex items-center justify-between hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors"
          :aria-expanded="openIndex === 3"
        >
          <span class="font-semibold text-gray-900 dark:text-white">
            What time zones do virtual assistants work in?
          </span>
          <svg class="w-5 h-5 text-gray-500 dark:text-gray-400 transform transition-transform"
               :class="openIndex === 3 ? 'rotate-180' : ''"
               fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M19 9l-7 7-7-7"></path>
          </svg>
        </button>

        <div x-show="openIndex === 3" x-collapse>
          <div class="px-6 pb-5 text-gray-600 dark:text-gray-400">
            Our virtual assistants work across multiple time zones. You can request coverage that matches
            your business hours or choose overlap hours for real-time collaboration.
          </div>
        </div>
      </div>

      <!-- Q5 -->
      <div class="bg-gray-50 dark:bg-gray-800 rounded-xl overflow-hidden border border-gray-200 dark:border-gray-700">
        <button
          @click="openIndex = openIndex === 4 ? null : 4"
          class="w-full px-6 py-5 text-left flex items-center justify-between hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors"
          :aria-expanded="openIndex === 4"
        >
          <span class="font-semibold text-gray-900 dark:text-white">
            Is my business data safe and confidential?
          </span>
          <svg class="w-5 h-5 text-gray-500 dark:text-gray-400 transform transition-transform"
               :class="openIndex === 4 ? 'rotate-180' : ''"
               fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M19 9l-7 7-7-7"></path>
          </svg>
        </button>

        <div x-show="openIndex === 4" x-collapse>
          <div class="px-6 pb-5 text-gray-600 dark:text-gray-400">
            Absolutely. We follow strict confidentiality policies, and NDAs can be signed upon request.
            Your data security and privacy are our top priorities.
          </div>
        </div>
      </div>

      <!-- Q6 -->
      <div class="bg-gray-50 dark:bg-gray-800 rounded-xl overflow-hidden border border-gray-200 dark:border-gray-700">
        <button
          @click="openIndex = openIndex === 5 ? null : 5"
          class="w-full px-6 py-5 text-left flex items-center justify-between hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors"
          :aria-expanded="openIndex === 5"
        >
          <span class="font-semibold text-gray-900 dark:text-white">
            What payment methods do you accept?
          </span>
          <svg class="w-5 h-5 text-gray-500 dark:text-gray-400 transform transition-transform"
               :class="openIndex === 5 ? 'rotate-180' : ''"
               fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M19 9l-7 7-7-7"></path>
          </svg>
        </button>

        <div x-show="openIndex === 5" x-collapse>
          <div class="px-6 pb-5 text-gray-600 dark:text-gray-400">
            We accept all major credit cards, debit cards, and online payment methods.
            Payments are billed monthly based on your selected plan.
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

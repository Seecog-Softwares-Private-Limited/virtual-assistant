<header class="sticky top-0 z-50 bg-white border-b border-gray-200">
  <nav class="container mx-auto px-6 h-20 flex items-center justify-between">

    <!-- Logo -->
    <a href="/" class="flex items-center gap-2">
      <span class="text-2xl font-bold text-purple-600">
        VA<span class="text-pink-500">Hub</span>
      </span>
    </a>

    <!-- Desktop Menu -->
    <div class="hidden md:flex items-center space-x-6">

      <a href="/" class="nav-link {{#if (eq activeRoute '/') }}text-purple-600 font-semibold{{/if}}">
        Home
      </a>

      <a href="/services" class="nav-link {{#if (eq activeRoute '/services') }}text-purple-600 font-semibold{{/if}}">
        Services
      </a>

      <a href="/pricing" class="nav-link {{#if (eq activeRoute '/pricing') }}text-purple-600 font-semibold{{/if}}">
        Pricing
      </a>

      <a href="/booking" class="nav-link {{#if (eq activeRoute '/booking') }}text-purple-600 font-semibold{{/if}}">
        Booking
      </a>

      <a href="/about" class="nav-link {{#if (eq activeRoute '/about') }}text-purple-600 font-semibold{{/if}}">
        About
      </a>

      {{!-- <!-- Dark Mode Toggle -->
      <button
        id="themeToggle"
        class="p-2 rounded-full hover:bg-gray-100 transition"
        aria-label="Toggle Dark Mode"
      >
        🌙
      </button> --}}

      <!-- Auth Links -->
      <a href="/login" class="nav-link">
        Login
      </a>

      <a href="/register" class="btn-secondary">
        Sign Up
      </a>

      <!-- CTA -->
      <a href="/booking" class="btn-primary">
        Hire a VA
      </a>
    </div>

    <!-- Mobile Menu Placeholder -->
    <div class="md:hidden">
      <button class="text-2xl">☰</button>
    </div>

  </nav>
</header>

<!-- Dark Mode Script -->
<script>
  const toggle = document.getElementById('themeToggle');

  toggle?.addEventListener('click', () => {
    document.documentElement.classList.toggle('dark');
  });
</script>

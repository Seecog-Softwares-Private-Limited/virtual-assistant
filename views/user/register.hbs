<section class="register-page min-h-screen bg-gradient-to-br from-purple-50 via-white to-pink-50 dark:from-gray-900 dark:via-purple-900/10 dark:to-gray-900 py-8 px-4 sm:px-6 lg:px-8 flex items-center justify-center relative overflow-hidden">
  <!-- Animated Background Elements -->
  <div class="register-background fixed inset-0 overflow-hidden pointer-events-none z-0">
    <div class="register-blob register-blob-1 absolute top-0 right-0 w-[500px] h-[500px] bg-purple-300 dark:bg-purple-900/20 rounded-full mix-blend-multiply dark:mix-blend-soft-light filter blur-3xl opacity-20 animate-blob"></div>
    <div class="register-blob register-blob-2 absolute bottom-0 left-0 w-[500px] h-[500px] bg-pink-300 dark:bg-pink-900/20 rounded-full mix-blend-multiply dark:mix-blend-soft-light filter blur-3xl opacity-20 animate-blob animation-delay-2000"></div>
    <div class="register-blob register-blob-3 absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-[400px] h-[400px] bg-purple-200 dark:bg-purple-800/20 rounded-full mix-blend-multiply dark:mix-blend-soft-light filter blur-3xl opacity-10 animate-blob animation-delay-4000"></div>
  </div>

  <!-- Main Container -->
  <div class="register-container max-w-2xl w-full relative z-10 mx-auto">
    
    <!-- Header Section -->
    <header class="register-header text-center mb-10 animate-fadeIn">
      <div class="register-logo inline-flex items-center justify-center w-24 h-24 bg-gradient-to-br from-purple-600 via-purple-500 to-pink-600 rounded-3xl mb-6 shadow-2xl transform hover:scale-110 transition-transform duration-300 ring-4 ring-purple-200/50 dark:ring-purple-800/30">
        <svg class="w-12 h-12 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
        </svg>
      </div>
      <h1 class="register-title text-4xl sm:text-5xl md:text-6xl font-extrabold text-gray-900 dark:text-white mb-4 bg-gradient-to-r from-purple-600 via-pink-600 to-purple-600 bg-clip-text text-transparent bg-300% animate-gradient">
        Join Stella
      </h1>
      <p class="register-subtitle text-base sm:text-lg md:text-xl text-gray-600 dark:text-gray-300 font-medium max-w-xl mx-auto leading-relaxed">
        Create your account to manage bookings and access exclusive features
      </p>
    </header>

    <!-- Registration Card -->
    <div class="register-card bg-white/95 dark:bg-gray-800/95 backdrop-blur-2xl rounded-3xl shadow-2xl border border-gray-200/60 dark:border-gray-700/60 p-6 sm:p-8 md:p-10 lg:p-12 animate-slideIn">
      
      <!-- Flash Messages -->
      {{#if error}}
        <div class="register-alert register-alert-error bg-gradient-to-r from-red-50 to-red-100/50 dark:from-red-900/30 dark:to-red-800/20 border-l-4 border-red-500 dark:border-red-400 rounded-xl p-4 flex items-start space-x-3 shadow-lg animate-fadeIn mb-6">
          <div class="register-alert-icon flex-shrink-0">
            <svg class="w-6 h-6 text-red-600 dark:text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <p class="register-alert-text text-sm font-semibold text-red-800 dark:text-red-200 flex-1">{{error}}</p>
        </div>
      {{/if}}

      {{#if success}}
        <div class="register-alert register-alert-success bg-gradient-to-r from-green-50 to-emerald-100/50 dark:from-green-900/30 dark:to-emerald-800/20 border-l-4 border-green-500 dark:border-green-400 rounded-xl p-4 flex items-start space-x-3 shadow-lg animate-fadeIn mb-6">
          <div class="register-alert-icon flex-shrink-0">
            <svg class="w-6 h-6 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <p class="register-alert-text text-sm font-semibold text-green-800 dark:text-green-200 flex-1">{{success}}</p>
        </div>
      {{/if}}

      <!-- Registration Form -->
      <form action="/register" method="POST" class="register-form space-y-6" id="registerForm">
        
        <!-- Name Field -->
        <div class="register-field">
          <label for="name" class="register-label">
            Full Name
          </label>
          <div class="register-input-wrapper">
            <div class="register-input-icon">
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
              </svg>
            </div>
            <input 
              type="text" 
              id="name" 
              name="name" 
              required
              autocomplete="name"
              class="register-input"
            />
          </div>
        </div>

        <!-- Email Field -->
        <div class="register-field">
          <label for="email" class="register-label">
            Email Address
          </label>
          <div class="register-input-wrapper">
            <div class="register-input-icon">
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207"></path>
              </svg>
            </div>
            <input 
              type="email" 
              id="email" 
              name="email" 
              required
              autocomplete="email"
              class="register-input"
            />
          </div>
        </div>

        <!-- Phone Field (Optional) -->
        <div class="register-field">
          <label for="phone" class="register-label">
            Phone Number 
            <span class="register-label-optional">(Optional)</span>
          </label>
          <div class="register-input-wrapper">
            <div class="register-input-icon">
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
              </svg>
            </div>
            <input 
              type="tel" 
              id="phone" 
              name="phone"
              autocomplete="tel"
              class="register-input"
            />
          </div>
        </div>

        <!-- Password Field -->
        <div class="register-field">
          <label for="password" class="register-label">
            Password
          </label>
          <div class="register-input-wrapper">
            <div class="register-input-icon">
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
              </svg>
            </div>
            <input 
              type="password" 
              id="password" 
              name="password" 
              required
              minlength="6"
              autocomplete="new-password"
              class="register-input"
            />
          </div>
          <p class="register-hint">
            <svg class="register-hint-icon" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
            </svg>
            <span class="register-hint-text">Must be at least 6 characters long</span>
          </p>
        </div>

        <!-- Confirm Password Field -->
        <div class="register-field">
          <label for="confirmPassword" class="register-label">
            Confirm Password
          </label>
          <div class="register-input-wrapper">
            <div class="register-input-icon">
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
              </svg>
            </div>
            <input 
              type="password" 
              id="confirmPassword" 
              name="confirmPassword" 
              required
              minlength="6"
              autocomplete="new-password"
              class="register-input"
            />
          </div>
        </div>

        <!-- Submit Button -->
        <button 
          type="submit" 
          class="register-submit-btn"
        >
          <span class="register-submit-content">
            <span>Create Account</span>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
            </svg>
          </span>
          <div class="register-submit-overlay"></div>
        </button>
      </form>

      <!-- Divider -->
      <div class="register-divider">
        <div class="register-divider-line"></div>
        <span class="register-divider-text">Already have an account?</span>
      </div>

      <!-- Login Link -->
      <a href="/login" class="register-login-link">
        <span class="register-login-content">
          <span>Sign In Instead</span>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"></path>
          </svg>
        </span>
      </a>

      <!-- Back to Home -->
      <div class="register-back-link">
        <a href="/" class="register-back-link-text">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
          </svg>
          <span>Back to Home</span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
/* ============================================
   REGISTER PAGE - PREMIUM CSS
   ============================================ */

/* Container Structure */
.register-page {
  position: relative;
  min-height: 100vh;
}

.register-container {
  width: 100%;
  max-width: 32rem;
  margin: 0 auto;
}

/* Header Section */
.register-header {
  margin-bottom: 2.5rem;
}

.register-logo {
  width: 6rem;
  height: 6rem;
  margin-bottom: 1.5rem;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #9333ea 0%, #a855f7 50%, #ec4899 100%);
  border-radius: 1.5rem;
  box-shadow: 0 25px 50px -12px rgba(147, 51, 234, 0.4);
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.register-logo:hover {
  transform: scale(1.1) rotate(5deg);
}

.register-title {
  font-size: 2.5rem;
  line-height: 1.1;
  font-weight: 800;
  margin-bottom: 1rem;
  background: linear-gradient(135deg, #9333ea 0%, #ec4899 50%, #9333ea 100%);
  background-size: 300% 300%;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: gradient-shift 3s ease infinite;
}

@media (min-width: 640px) {
  .register-title {
    font-size: 3rem;
  }
}

@media (min-width: 768px) {
  .register-title {
    font-size: 3.75rem;
  }
}

.register-subtitle {
  font-size: 1rem;
  line-height: 1.75;
  color: #4b5563;
  max-width: 36rem;
  margin: 0 auto;
}

@media (min-width: 640px) {
  .register-subtitle {
    font-size: 1.125rem;
  }
}

@media (min-width: 768px) {
  .register-subtitle {
    font-size: 1.25rem;
  }
}

.dark .register-subtitle {
  color: #d1d5db;
}

/* Registration Card */
.register-card {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-radius: 1.5rem;
  box-shadow: 
    0 20px 25px -5px rgba(0, 0, 0, 0.1),
    0 10px 10px -5px rgba(0, 0, 0, 0.04),
    0 0 0 1px rgba(147, 51, 234, 0.05);
  border: 1px solid rgba(229, 231, 235, 0.6);
  padding: 1.5rem;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

@media (min-width: 640px) {
  .register-card {
    padding: 2rem;
  }
}

@media (min-width: 768px) {
  .register-card {
    padding: 2.5rem;
  }
}

@media (min-width: 1024px) {
  .register-card {
    padding: 3rem;
  }
}

.dark .register-card {
  background: rgba(31, 41, 55, 0.95);
  border-color: rgba(55, 65, 81, 0.6);
}

.register-card:hover {
  box-shadow: 
    0 25px 50px -12px rgba(0, 0, 0, 0.15),
    0 0 0 1px rgba(147, 51, 234, 0.1);
}

/* Alert Messages */
.register-alert {
  border-radius: 0.75rem;
  padding: 1rem;
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
  margin-bottom: 1.5rem;
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
}

.register-alert-icon {
  flex-shrink: 0;
}

.register-alert-text {
  font-size: 0.875rem;
  font-weight: 600;
  flex: 1;
}

/* Form Structure */
.register-form {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

/* Form Field */
.register-field {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

/* Label */
.register-label {
  display: block;
  font-size: 0.875rem;
  font-weight: 700;
  color: #374151;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 0.5rem;
}

.dark .register-label {
  color: #e5e7eb;
}

.register-label-optional {
  font-size: 0.75rem;
  font-weight: 400;
  color: #6b7280;
  text-transform: none;
  letter-spacing: normal;
}

.dark .register-label-optional {
  color: #9ca3af;
}

/* Input Wrapper */
.register-input-wrapper {
  position: relative;
  display: flex;
  align-items: center;
}

.register-input-icon {
  position: absolute;
  left: 1rem;
  top: 50%;
  transform: translateY(-50%);
  z-index: 10;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 1.25rem;
  height: 1.25rem;
  color: #9ca3af;
  pointer-events: none;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

/* Ensure icon doesn't interfere with text input */
.register-input-icon + .register-input {
  padding-left: 3.5rem;
}

.register-input-icon svg {
  width: 100%;
  height: 100%;
  display: block;
}

.register-input-wrapper:focus-within .register-input-icon {
  color: #9333ea;
  transform: translateY(-50%) scale(1.1);
}

.dark .register-input-wrapper:focus-within .register-input-icon {
  color: #a78bfa;
}

.register-input-wrapper:hover:not(:focus-within) .register-input-icon {
  color: #6b7280;
}

.dark .register-input-wrapper:hover:not(:focus-within) .register-input-icon {
  color: #9ca3af;
}

/* Input Field */
.register-input {
  width: 100%;
  padding: 0.875rem 1rem 0.875rem 3.5rem;
  border-radius: 0.75rem;
  border: 2px solid #e5e7eb;
  background: #ffffff;
  color: #111827;
  font-size: 1rem;
  line-height: 1.5;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  text-indent: 0;
}

.register-input::placeholder {
  color: #9ca3af;
}

.register-input:hover:not(:focus) {
  border-color: rgba(147, 51, 234, 0.3);
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}

.register-input:focus {
  outline: none;
  border-color: #9333ea;
  box-shadow: 
    0 0 0 4px rgba(147, 51, 234, 0.1),
    0 10px 15px -3px rgba(0, 0, 0, 0.1);
  transform: translateY(-1px);
}

.dark .register-input {
  background: rgba(55, 65, 81, 0.5);
  border-color: #374151;
  color: #ffffff;
}

.dark .register-input::placeholder {
  color: #6b7280;
}

.dark .register-input:hover:not(:focus) {
  border-color: rgba(168, 85, 247, 0.3);
}

.dark .register-input:focus {
  border-color: #a78bfa;
  box-shadow: 
    0 0 0 4px rgba(168, 85, 247, 0.1),
    0 10px 15px -3px rgba(0, 0, 0, 0.1);
}

.register-input:invalid:not(:placeholder-shown) {
  border-color: #ef4444;
}

.dark .register-input:invalid:not(:placeholder-shown) {
  border-color: #dc2626;
}

/* Hint Text */
.register-hint {
  display: flex;
  align-items: center;
  gap: 0.375rem;
  font-size: 0.625rem;
  line-height: 1.4;
  color: #9ca3af;
  margin-top: 0.375rem;
  opacity: 0;
  transform: translateY(-4px);
  animation: hint-fade-in 0.4s ease-out 0.2s forwards;
}

.dark .register-hint {
  color: #6b7280;
}

.register-hint-icon {
  width: 0.875rem;
  height: 0.875rem;
  flex-shrink: 0;
  color: #9333ea;
  animation: hint-icon-pulse 2s ease-in-out infinite;
}

.dark .register-hint-icon {
  color: #a78bfa;
}

.register-hint-text {
  font-weight: 500;
  letter-spacing: 0.025em;
  animation: hint-text-shimmer 3s ease-in-out infinite;
}

@keyframes hint-fade-in {
  from {
    opacity: 0;
    transform: translateY(-4px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes hint-icon-pulse {
  0%, 100% {
    opacity: 1;
    transform: scale(1);
  }
  50% {
    opacity: 0.7;
    transform: scale(1.1);
  }
}

@keyframes hint-text-shimmer {
  0%, 100% {
    opacity: 0.8;
  }
  50% {
    opacity: 1;
  }
}

/* Submit Button */
.register-submit-btn {
  position: relative;
  width: 100%;
  padding: 1rem 1.5rem;
  margin-top: 1.5rem;
  background: linear-gradient(135deg, #9333ea 0%, #a855f7 50%, #ec4899 100%);
  color: #ffffff;
  font-weight: 700;
  font-size: 1rem;
  border-radius: 0.75rem;
  border: none;
  cursor: pointer;
  overflow: hidden;
  box-shadow: 
    0 10px 15px -3px rgba(147, 51, 234, 0.3),
    0 4px 6px -2px rgba(147, 51, 234, 0.2);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.register-submit-btn:hover {
  transform: translateY(-2px) scale(1.02);
  box-shadow: 
    0 20px 25px -5px rgba(147, 51, 234, 0.4),
    0 10px 10px -5px rgba(147, 51, 234, 0.2);
}

.register-submit-btn:active {
  transform: translateY(0) scale(0.98);
}

.register-submit-btn:focus {
  outline: none;
  box-shadow: 
    0 0 0 4px rgba(147, 51, 234, 0.3),
    0 20px 25px -5px rgba(147, 51, 234, 0.4);
}

.register-submit-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
}

.register-submit-content {
  position: relative;
  z-index: 10;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
}

.register-submit-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, #7e22ce 0%, #be185d 50%, #7e22ce 100%);
  opacity: 0;
  transition: opacity 0.3s ease;
}

.register-submit-btn:hover .register-submit-overlay {
  opacity: 1;
}

.register-submit-btn svg {
  transition: transform 0.3s ease;
}

.register-submit-btn:hover svg {
  transform: translateX(4px);
}

/* Divider */
.register-divider {
  position: relative;
  margin: 2rem 0;
  display: flex;
  align-items: center;
}

.register-divider-line {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
}

.register-divider-line::before {
  content: '';
  width: 100%;
  height: 2px;
  background: #e5e7eb;
}

.dark .register-divider-line::before {
  background: #374151;
}

.register-divider-text {
  position: relative;
  padding: 0 1rem;
  font-size: 0.875rem;
  font-weight: 500;
  color: #6b7280;
  background: rgba(255, 255, 255, 0.95);
}

.dark .register-divider-text {
  color: #9ca3af;
  background: rgba(31, 41, 55, 0.95);
}

/* Login Link */
.register-login-link {
  display: block;
  width: 100%;
  text-align: center;
  padding: 1rem 1.5rem;
  background: #ffffff;
  color: #111827;
  font-weight: 700;
  font-size: 1rem;
  border-radius: 0.75rem;
  border: 2px solid #d1d5db;
  text-decoration: none;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.register-login-link:hover {
  border-color: #9333ea;
  background: rgba(250, 245, 255, 1);
  transform: translateY(-2px) scale(1.02);
  box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
}

.register-login-link:active {
  transform: translateY(0) scale(0.98);
}

.dark .register-login-link {
  background: #374151;
  color: #ffffff;
  border-color: #4b5563;
}

.dark .register-login-link:hover {
  border-color: #a78bfa;
  background: rgba(88, 28, 135, 0.2);
}

.register-login-content {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
}

.register-login-link svg {
  transition: transform 0.3s ease;
}

.register-login-link:hover svg {
  transform: translateX(4px);
}

/* Back Link */
.register-back-link {
  text-align: center;
  padding-top: 1rem;
  margin-top: 1rem;
}

.register-back-link-text {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 0.875rem;
  font-weight: 500;
  color: #4b5563;
  text-decoration: none;
  transition: color 0.3s ease;
}

.register-back-link-text:hover {
  color: #9333ea;
}

.dark .register-back-link-text {
  color: #9ca3af;
}

.dark .register-back-link-text:hover {
  color: #a78bfa;
}

.register-back-link-text svg {
  transition: transform 0.3s ease;
}

.register-back-link-text:hover svg {
  transform: translateX(-4px);
}

/* Animations */
@keyframes gradient-shift {
  0%, 100% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
}

/* Responsive Adjustments */
@media (max-width: 640px) {
  .register-card {
    padding: 1.25rem;
  }
  
  .register-title {
    font-size: 2rem;
  }
  
  .register-subtitle {
    font-size: 0.9375rem;
  }
  
  .register-logo {
    width: 5rem;
    height: 5rem;
  }
}

/* Accessibility */
.register-input:focus-visible,
.register-submit-btn:focus-visible,
.register-login-link:focus-visible {
  outline: 2px solid #9333ea;
  outline-offset: 2px;
}

/* Print Styles */
@media print {
  .register-background,
  .register-blob {
    display: none;
  }
  
  .register-card {
    box-shadow: none;
    border: 1px solid #e5e7eb;
  }
}
</style>
